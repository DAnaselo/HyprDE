### Monitors
monitorv2 {
  output = DP-3
  mode = 1920x1080@180
  scale = 1
  supports_wide_color = 1 
  supports_hdr = 0
}

render {
  direct_scanout = 1
}

### Autostart
exec-once = dbus-update-activation-environment --systemd --all
exec-once = systemctl --user import-environment QT_QPA_PLATFORMTHEME
exec-once = waybar & hyprpaper & hypridle & hyprsunset
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
exec-once = syncthing --no-browser
exec-once = udiskie --tray
#exec-once = steam -silent
exec-once = wl-paste --type text --watch cliphist store  & # Starts Cliphist With text Headers Exposed
exec-once = wl-paste --type image --watch cliphist store & # Starts Cliphist With image Headers Exposed

### Environment Varibles
env = HYPRCURSOR_THEME,macOS-hypr
env = HYPRCURSOR_SIZE,32
env = LIBVA_DRIVER_NAME,nvidia # Makes The Default VAAPI Driver The Nvidia Driver, You probably want this
env = XDG_SESSION_TYPE,wayland # Sets The Session To Be Wayland in xdg, You probably want this
env = ELECTRON_OZONE_PLATFORM_HINT,auto # Hint Electrone Apps To Use Wayland, If They Can't They'll Fall back To X11
env = GBM_BACKEND,nvidia-drm # Sets The GMB Buffer API To Use The Nvidia-drm Driver, YOU NEED THIS
env = __GLX_VENDOR_LIBRARY_NAME,nvidia # OpenGL implementation being used is provided by NVIDIA, Thx Gemini
env = NVD_BACKEND,direct # Is Used For Directly Hooking Into NVDEC And NVENC, Useful for Hardware Accelerated Decoding and Encoding
env = QT_SCALE_FACTOR=1.25 # Sets The QT Scaling To 1.25
env = QT_QPA_PLATFORM="wayland;xcb" # Sets The QT Plugins To Prefer wayland.. If they can't they will fall back to x11
env = QT_QPA_PLATFORMTHEME=hyprqt6engine
env = GDK_SCALE=1.25 #$ Sets The GDK Scale To 1.25
env = GDK_BACKEND=wayland,x11 # Tries To Make GDK Apps to use Wayland.. If it can't it will fallback to x11

general {
	gaps_in = 2
	gaps_out = 0
	border_size = 0
	resize_on_border = true
	layout = dwindle
	allow_tearing = false
}

decoration {
	rounding = 5
	shadow {
		enabled = yes
		range = 4
		render_power = 3
		color = rgba(1a1a1aee)
	}
	blur {
		enabled = yes
		size = 3
		passes = 1
		vibrancy = 0.1696
	}
}

curve {
  bezier = easeInOutExpo,0.16,1,0.3,1
  bezier = linear,0,0,1,1
  bezier = almostLinear,0.5,0.5,0.75,1.0
  bezier = quick,0.15,0,0.1,1
  animation = global, 1, 10, default
  animation = border, 1, 5.39, easeInOutExpo  
  animation = windows, 1, 4.79, easeInOutExpo
  animation = windowsIn, 1, 4.1, easeInOutExpo, popin 89%
  animation = windowsOut, 1, 1.49, linear, popin 90%
  animation = fadeIn, 1, 1.73, almostLinear
  animation = fadeOut, 1, 1.46, almostLinear
  animation = fade, 1, 3.03, quick
  animation = layers, 1, 3.81, easeInOutExpo
  animation = layersIn, 1, 4, easeInOutExpo, fade
  animation = layersOut, 1, 1.5, linear, fade
  animation = fadeLayersIn, 1, 1.79, almostLinear
  animation = fadeLayersOut, 1, 1.39, almostLinear
  animation = workspaces, 1, 1.94, easeInOutExpo, fade
  animation = workspacesIn, 1, 1.21, almostLinear, fade
  animation = workspacesOut, 1, 1.94, almostLinear, fade 
}

dwindle {
	preserve_split = yes # you probably want this
}

master {
	new_status = true # i don't really know what this does smh, go the wiki.. too lazy to go check
}

misc {
	force_default_wallpaper = 0 # Set to 0 to disable the anime mascot wallpapers
	disable_hyprland_logo = true # NO LOGO FOR ME
	vrr = 2
  #mouse_move_enables_dpms = true
  #key_press_enables_dpms = true
}

### Input
input {
  repeat_rate = 50
  repeat_delay = 350
  follow_mouse = 1
	kb_layout = us,ara
	kb_options = grp:alt_shift_toggle
  accel_profile = flat
}

### keybinds
$mainMod = SUPER
bind = $ALT, RETURN, exec, kitty
bind = $mainMod SHIFT, RETURN, exec, kitty tmux a
bind = $mainMod, Q, killactive,
bind = $mainMod SHIFT, Q, exit,
bind = $mainMod, E, exec, pcmanfm
bind = $mainMod SHIFT, D, exec, kitty danavi
bind = $mainMod, V, togglefloating,
bind = CTRL, Space, global, :main
bind = $ALT, SPACE, exec, rofi -show drun # rofi
bind = $mainMod, W, exec, xdg-open https:// # Open's Up The Default Xdg Browser
bind = $mainMod, A, exec, waybar
bind = $mainMod, F, fullscreen
bind = SUPER, Space, exec, pgrep -x waystt >/dev/null && pkill --signal SIGUSR1 waystt || (waystt --pipe-to ydotool type --file - &)
bind = SUPER SHIFT, Space, exec, pgrep -x waystt >/dev/null && pkill --signal SIGUSR1 waystt || (waystt --pipe-to wl-copy &)
#bind = $mainMod, O, exec, hyprctl dispatch dpms off
bind = $mainMod, N, exec, hyprctl dispatch dpms on
bind = $mainMod, L, exec, systemctl suspend
bind = $mainMod, B, exec, kitty btop
bind = $mainMod, D, exec, flatpak run org.localsend.localsend_app
bind = $mainMod, K, exec, flatpak run org.kde.kdenlive
bind = $mainMod, X, exec, $HOME/HyprDE/Scripts/undervolter.sh
bind = $mainMod SHIFT, X, exec, $HOME/HyprDE/Scripts/0power.sh
bind = $mainMod, Z, exec, $HOME/HyprDE/Scripts/audio.sh
bind = $mainMod, R, exec, $HOME/Documents/librepods/linux/build/librepods
bind = $mainMod, P, exec, $HOME/HyprDE/Scripts/screenshot.sh 
bind = $mainMod SHIFT, A, exec, pkill -9 waybar
bind = $mainMod, Tab, exec, $HOME/orrr.sh
bind = $mainMod, J, togglesplit,
bind = $mainMod, C, exec, cliphist list | rofi -dmenu | cliphist decode | wl-copy # Pull's a Clipboard Manager Gui made With the power of rofi and cliphist
bind = $mainMod SHIFT, C, exec, cliphist wipe | hyprctl notify 3 3000 0 " Clipboard Wiped" # Wipes Clipboard history
#bind = $mainMod, I, exec, hyprctl notify 1 3000 0 " Hypridle Started" | hypridle # Starts Hypridle
bind = $mainMod SHIFT, I, exec, pkill -9 hypridle | hyprctl notify 4 3000 0 " Hypridle Stopped" # Stops Hypridle
#bind = $mainMod, U, exec, hyprsunset | hyprctl notify 1 3000 0 " Hyprsunset Started"
bind = $mainMod SHIFT, U, exec, pkill -9 hyprsunset | hyprctl notify 4 3000 0 " Hyprsunset Stopped"
bind = SUPER, G, exec, [workspace 2] hyprctl notify 1 5000 0 " Gamescope Steam Started" && pkill -9 steam || true && ENABLE_GAMESCOPE_WSI=0 gamescope -W 1920 -H 1080 -r 180 --steam --force-grab-cursor --mangoapp -f -- steam -steamos3 -steamdeck
bindel = , XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle # Mute's Audio via wireplumber
bindel = , XF86AudioRaiseVolume, exec, wpctl set-volume -l 1.0 @DEFAULT_AUDIO_SINK@ 2%+ # Increases Audio by 2 via wireplumber
bindel = , XF86AudioLowerVolume, exec, wpctl set-volume -l 1.0 @DEFAULT_AUDIO_SINK@ 2%- # Decreases Audio by 2 via wireplumber

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Fixing F-Keys
#bind = , code:232, exec, wtype -k F1
#bind = , code:233, exec, wtype -k F2
#bind = , code:128, exec, wtype -k F3
#bind = , code:212, exec, wtype -k F4
#bind = , code:237, exec, wtype -k F5
#bind = , code:238, exec, wtype -k F6
#bind = , code:173, exec, wtype -k F7
#bind = , code:172, exec, wtype -k F8
#bind = , code:171, exec, wtype -k F9
#bind = , code:121, exec, wtype -k F10
#bind = , code:122, exec, wtype -k F11
#bind = , code:123, exec, wtype -k F12

# Assigning Workspaces To Monitors
workspace = 1, monitor:DP-3
workspace = 2, monitor:DP-3
workspace = 3, monitor:DP-3
workspace = 4, monitor:DP-3
workspace = 5, monitor:DP-3

# Switch workspaces with mainMod + [0-5]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5

# Move active window to a workspace with mainMod + SHIFT + [0-5]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

### Workspace and rules
windowrule = no_blur on, match:title steam
windowrule = no_blur on, match:class resolve
windowrule = float on, size 1271 771, match:class xdg-desktop-portal-gtk
windowrule = float on, match:class xdg-desktop-portal-hyprland
windowrule = float on, size 588 441, match:class localsend
windowrule = float on, match:class org.gnome.Loupe
windowrule = float on, match:class LosslessCut
windowrule = float on, match:class it.mijorus.gearlever
windowrule = float on, match:class gay.pancake.lsfg-vk-ui
windowrule = float on, match:class com.vysp3r.ProtonPlus
windowrule = float on, match:class nwg-look
windowrule = float on, match:class piper
windowrule = float on, size 997 673, match:class org.keepassxc.KeePassXC
windowrule = float on, size 1307 707, match:class org.prismlauncher.PrismLauncher
windowrule = float on, size 1337 724, match:class io.missioncenter.MissionCenter
windowrule = float on, match:class mpv
windowrule = float on, match:class zenity
windowrule = float on, size 1149 665, match:class pcmanfm
windowrule = float on, size 398 321, match:class me.kavishdevar.librepods
windowrule = float on, size 536 281, match:class blueman-manager
windowrule = float on, match:title Splash Screen — Kdenlive
windowrule = move 376 191, size 1089 635, match:initial_title Kdenlive
